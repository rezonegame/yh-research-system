interface:
  display_name: "Skill Factory"
  short_description: "Create, validate, and retire skills"
  default_prompt: "Use $skill-factory to create, update, validate, or retire a local skill or deep person perspective in .claude/skills."
policy:
  allow_implicit_invocation: true
