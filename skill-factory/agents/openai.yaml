interface:
  display_name: "Skill Factory"
  short_description: "Create and retire reusable local skills"
  default_prompt: "Use $skill-factory to create, update, or retire a self-contained local skill in .claude/skills."
policy:
  allow_implicit_invocation: true
